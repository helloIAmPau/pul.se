scalar UUID
scalar StreamState
scalar Timestamp
scalar URL

type Stream {
  app: UUID!,
  name: String!,
  url: URL!,
  state: StreamState!,
  timestamp: Timestamp!
}

type Query {
  stream(app: UUID!): Stream
  live: [ Stream ]!
}
