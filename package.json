{
  "name": "@pul.se/repo",
  "version": "0.0.0",
  "scripts": {
    "start": "docker compose up --build",
    "develop": "source ./.env.develop && docker compose -f docker-compose.yml -f docker-compose.develop.yml up --build",
    "migrate": "docker compose exec -T postgres psql -U ${POSTGRES_USER} -d ${POSTGRES_DB} < migrations.sql"
  },
  "workspaces": [
    "./workspaces/@pul.se/*"
  ]
}
